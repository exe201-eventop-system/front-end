
const Profile = () => {
 // const { data: posts = [] } = useGetPostsQuery();
return (
    <div className="p-3 m-1.5">
      {/* <h1 className="text-3xl font-bold mb-6 text-center">Danh sách bài viết</h1>
      <ul className="grid gap-4">
        {posts.slice(0, 10).map((post: Post) => (
          <li key={post.id} className="bg-white rounded-xl p-5 shadow hover:shadow-lg transition">
            <h2 className="text-xl font-semibold mb-2">{post.title}</h2>
            <p className="text-gray-700">{post.body}</p>
          </li>
        ))}
      </ul> */}
      này là trang   profile
    </div>
  );
};

export default Profile;
